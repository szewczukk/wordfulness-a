{
	"license": "UNLICENSED",
	"scripts": {
		"test": "cd frontend && yarn test",
		"eslint": "cd frontend && yarn eslint && cd ../backend && yarn eslint"
	},
	"devDependencies": {
		"husky": "^7.0.4"
	},
	"graphql": {
		"schema": "./backend/schema.gql"
	}
}
