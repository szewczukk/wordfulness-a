{
	 "license": "UNLICENSED",
  "scripts": {
    "test": "cd frontend && yarn test",
		"linter": "cd frontend && yarn linter && cd ../backend && yarn linter"
  },
  "devDependencies": {
    "husky": "^7.0.4"
  },
  "graphql": {
    "schema": "./backend/schema.gql"
  }
}
