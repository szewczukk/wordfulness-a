mutation CreateFlashcard($front: String!, $back: String!, $lessonId: ID!) {
	createFlashcard(
		createFlashcardInput: { front: $front, back: $back, lessonId: $lessonId }
	) {
		id
		front
		back
	}
}
